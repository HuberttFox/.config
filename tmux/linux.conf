# ==========================================
# Tmux config for Linux
# ==========================================

# Shared interaction defaults.
set -g mouse on
set -s escape-time 0
# Forward terminal focus changes to pane programs such as editors and TUIs.
set -s focus-events on
# Preserve richer key combinations from modern terminals.
set -s extended-keys on
# Exit the tmux server when the last session is gone.
set -sg exit-empty on
# Keep repeatable key bindings responsive without a long timeout window.
set -sg repeat-time 300
# Detach the client cleanly if its target is destroyed.
set -g detach-on-destroy on
# Allow supported escape sequences to pass through tmux when needed.
set -g allow-passthrough on
# Refresh a small set of session-dependent variables when reattaching clients.
set -g update-environment "SSH_AUTH_SOCK DISPLAY DBUS_SESSION_BUS_ADDRESS XDG_RUNTIME_DIR"
# Quietly keep UTF-8 options enabled for compatibility with older configs.
set -q -g status-utf8 on
setw -q -g utf8 on
# Mouse wheel should scroll tmux history in copy mode.
bind -T copy-mode-vi WheelUpPane select-pane \; send-keys -X -N 1 scroll-up
bind -T copy-mode-vi WheelDownPane select-pane \; send-keys -X -N 1 scroll-down
# Large enough scrollback for logs without keeping an excessive buffer.
set -g history-limit 10000
# Keep status messages visible briefly without overstaying.
set -g display-time 2000
# Use tmux's own terminfo entry so applications can detect richer capabilities.
set -g default-terminal "tmux-256color"
# Advertise extended key support for xterm-compatible terminals.
set -as terminal-features 'xterm*:extkeys'

# GNU Screen-style primary prefix, while still accepting the tmux default.
set -g prefix C-a
set -g prefix2 C-b
# Send the literal prefix through to programs when needed.
bind C-a send-prefix

# Start numbering at 1 and keep window names aligned with the current command.
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
setw -g automatic-rename on
# Surface background activity with visible window markers instead of just bells.
set -g visual-activity on
setw -g monitor-activity on
setw -g monitor-bell on

# Reload config in place and keep new splits rooted in the current path.
bind r source-file ~/.tmux.conf \; display-message "config reloaded"
# Future hook for integrating an external client tracker on attach.
# set-hook -gu client-attached
# set-hook -g client-attached 'run -b "test -x ~/.config/agent-tracker/bin/tracker-client && ~/.config/agent-tracker/bin/tracker-client command acknowledge --client #{client_tty} --session-id #{session_id} --window-id #{window_id} --pane #{pane_id} || true"'
# Future hook to refresh the client after the tracker acknowledgement hook runs.
# set-hook -ag client-attached 'run -b "tmux refresh-client -S"'
# Future pane-focus hook stack for MRU panes, tracker acknowledgements, unread reset, and UI refresh.
# set-hook -gu pane-focus-in
# set-hook -g pane-focus-in "run -b 'bash ~/.config/tmux/fzf_panes.tmux update_mru_pane_ids'"
# set-hook -ag pane-focus-in 'run -b "test -x ~/.config/agent-tracker/bin/tracker-client && ~/.config/agent-tracker/bin/tracker-client command acknowledge --client #{client_tty} --session-id #{session_id} --window-id #{window_id} --pane #{pane_id} || true"'
# set-hook -ag pane-focus-in 'run -b "tmux set -wu -t #{window_id} @unread 2>/dev/null || true"'
# set-hook -ag pane-focus-in 'run -b "tmux refresh-client -S"'
# Future pane-died hook stack for tracker cleanup and pane archival bookkeeping.
# set-hook -gu pane-died
# set-hook -g pane-died 'run -b "test -x ~/.config/agent-tracker/bin/tracker-client && ~/.config/agent-tracker/bin/tracker-client command delete_task --client #{client_tty} --session-id #{session_id} --window-id #{window_id} --pane #{pane_id} || true"'
# set-hook -ag pane-died 'run -b "test -x ~/.config/agent-tracker/bin/tracker-client && ~/.config/agent-tracker/bin/tracker-client command note_archive_pane --client #{client_tty} --session-id #{session_id} --window-id #{window_id} --pane #{pane_id} || true"'
# Future after-select-window hook for running per-directory activation logic.
# set-hook -gu after-select-window
# set-hook -g after-select-window 'run-shell "~/.config/tmux/scripts/check_and_run_on_activate.sh \"#{pane_current_path}\""'
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
# Vim-style pane navigation and repeatable resizing.
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

setw -g mode-keys vi
# Use Vim-like selection flow inside copy mode.
bind -T copy-mode-vi v send -X begin-selection
# Linux keeps copy-mode self-contained by copying into tmux's buffer.
bind -T copy-mode-vi y send -X copy-selection-and-cancel
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-selection-and-cancel

# Minimal status bar with session on the left and time on the right.
set -g status-interval 1
set -g status-justify left
set -g status-style "bg=#282c34,fg=#abb2bf"
set -g status-left-length 30
set -g status-left "#[fg=green,bold] #S #[fg=white]| "
set -g status-right-length 100
set -g status-right "#[fg=cyan] %H:%M:%S #[fg=yellow] %Y-%m-%d "
setw -g window-status-current-style "fg=#282c34,bg=#61afef,bold"
setw -g window-status-current-format " #I:#W#F "

# Plugin stack: sane defaults, theme, and session persistence.
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'catppuccin/tmux'
set -g @catppuccin_flavour 'macchiato'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @continuum-restore 'on'

# TPM loads and manages the plugin list above.
run '~/.tmux/plugins/tpm/tpm'
